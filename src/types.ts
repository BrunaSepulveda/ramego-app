export type BrothProtein = {
  id: string;
  imageInactive: string;
  imageActive: string;
  name: string;
  description: string;
  price: number;
};

export type Order = {
  id: string;
  image: string;
  description: string;
};
